defaults:
  runner: codex
  service: acquiring
  tags:
    - go
    - exchange_buy

available:
  target_branch:
    - u1
    - u1-1
    - u1-4
  namespace:
    - acquiring-u1
    - acquiring-u1-1
    - acquiring-u1-4

# Optional: override defaults/available for params declared in tag params.yaml files
# tag_params:
#   exchange_buy:
#     defaults:
#       dataset: s4-3ds
#     available:
#       dataset:
#         - s4-3ds
#         - s4-no3ds
